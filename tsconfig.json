/**
 * es2016 Array.prototype.includes 幂运算: 2 ** 3
 * es2017 异步函数(Async/Await) 共享内存和原子(Shared memory and atomics) 
 * Object.values() Object.entries() 字符串填充(padStart 和 padEnd) 
 * Object.getOwnPropertyDescriptors() 尾逗号(Trailing commas)
 */
{
    // https://www.tslang.cn/docs/handbook/compiler-options.html
    "compilerOptions": {
        "module": "commonjs",
        "target": "es2017", // 目标代码版本
        "removeComments": true,
        "preserveConstEnums": true, // 生成的代码不删除常量枚举
        "moduleResolution": "node", // 模块解析策略
        "experimentalDecorators": true, // 启用实验性的ES装饰器。
        "emitDecoratorMetadata": true, // 给装饰器声明加上设计类型元数据
        "sourceMap": true,
        "sourceRoot": "src",
        "outDir": "dist",
        "baseUrl": "."
    },
    "include": [
        "src/**/*"
    ],
    "exclude": [
        "node_modules"
    ]
}